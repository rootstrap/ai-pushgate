# =============================================================================
# Pushgate v2 configuration - Next.js
# https://github.com/rootstrap/ai-pushgate
# =============================================================================

version: 2

ai:
  mode: blocking
  verbose: true
  max_changed_lines: 500
  max_prompt_tokens: 12000
  timeout_seconds: 120
  provider: claude
  providers:
    claude:
      model: claude-sonnet-4-20250514

review:
  target_branch: main
  context_lines: 10
  max_lines_for_full_file: 300

tools:
  - name: tsc
    command: ["npx", "tsc", "--noEmit"]
    extensions: [".ts", ".tsx"]

  - name: eslint
    # Uses Next.js built-in ESLint config
    command: ["npx", "next", "lint", "--file", "{changed_files}"]
    extensions: [".ts", ".tsx", ".js", ".jsx"]

  - name: prettier
    command: ["npx", "prettier", "--check", "{changed_files}"]
    extensions: [".ts", ".tsx", ".js", ".jsx", ".json", ".css", ".scss", ".md"]

  - name: jest
    command: ["npx", "jest", "--passWithNoTests", "--findRelatedTests", "{changed_files}"]
    extensions: [".ts", ".tsx", ".js", ".jsx"]

ignore_paths:
  - "*.lock"
  - "package-lock.json"
  - "pnpm-lock.yaml"
  - "yarn.lock"
  - ".next/**"
  - "dist/**"
  - "build/**"
  - "coverage/**"
  - "*.d.ts"
  - "public/**"
