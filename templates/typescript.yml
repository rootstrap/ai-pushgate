# =============================================================================
# Pushgate v2 configuration - TypeScript
# https://github.com/rootstrap/ai-pushgate
# =============================================================================

version: 2

ai:
  mode: blocking
  verbose: true
  max_changed_lines: 500
  max_prompt_tokens: 12000
  timeout_seconds: 120
  provider: claude
  providers:
    claude:
      model: claude-sonnet-4-20250514

review:
  target_branch: main
  context_lines: 10
  max_lines_for_full_file: 300

tools:
  - name: tsc
    command: ["npx", "tsc", "--noEmit"]
    extensions: [".ts", ".tsx"]

  - name: eslint
    command: ["npx", "eslint", "{changed_files}"]
    extensions: [".ts", ".tsx", ".js", ".jsx", ".mjs"]

  - name: prettier
    command: ["npx", "prettier", "--check", "{changed_files}"]
    extensions: [".ts", ".tsx", ".js", ".jsx", ".mjs", ".json", ".css", ".md"]

  - name: jest
    command: ["npx", "jest", "--passWithNoTests", "--findRelatedTests", "{changed_files}"]
    extensions: [".ts", ".tsx", ".js", ".jsx"]

ignore_paths:
  - "*.lock"
  - "package-lock.json"
  - "pnpm-lock.yaml"
  - "yarn.lock"
  - "dist/**"
  - "build/**"
  - "coverage/**"
  - "*.d.ts"
